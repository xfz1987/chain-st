# ChainST 区块链 - 快速开始指南

恭喜!你的 Cosmos SDK 区块链项目已经准备就绪。

## 📁 项目位置

```
~/work/web3/chain-st/
├── chainst/              # 主区块链项目
├── explorer/             # Web 区块链浏览器
├── TUTORIAL.md           # 详细教程
└── 快速开始.md           # 本文件
```

## 🚀 立即开始

### 方法 1: 一键启动(推荐)

```bash
cd ~/work/web3/chain-st/chainst
ignite chain serve
```

### 方法 2: 分步启动

```bash
# 1. 进入项目目录
cd ~/work/web3/chain-st/chainst

# 2. 安装依赖
go mod tidy

# 3. 启动区块链
ignite chain serve
```

启动成功后,你会看到类似这样的输出:

```
✔ Building proto...
✔ Building blockchain...
✔ Imported accounts...
✔ Running blockchain...

🌍 Tendermint node: http://localhost:26657
🌍 Blockchain API: http://localhost:1317
🌍 Token faucet: http://localhost:4500
```

## 🔍 访问区块链浏览器

启动区块链后,在浏览器中打开:

**方法 1: 直接打开 HTML 文件**

```
file:///Users/autonomic/work/web3/chain-st/explorer/index.html
```

**方法 2: 使用 HTTP 服务器(推荐)**

```bash
# 新开一个终端
cd ~/work/web3/chain-st/explorer
python3 -m http.server 8000

# 然后在浏览器访问:
# http://localhost:8000
```

## 📝 测试功能

### 1. 查看账户

```bash
chainstd keys list
```

你会看到两个测试账户:

- alice
- bob

### 2. 查询余额

```bash
chainstd query bank balances $(chainstd keys show alice -a)
```

### 3. 铸造代币

```bash
chainstd tx token mint-tokens 1000000 mytoken \
  --from alice \
  --chain-id chainst \
  --yes
```

### 4. 转账代币

```bash
chainstd tx token transfer-tokens \
  $(chainstd keys show bob -a) \
  100000 \
  mytoken \
  --from alice \
  --chain-id chainst \
  --yes
```

### 5. 查看最新区块

```bash
chainstd query block
```

## 🌐 API 端点

区块链提供以下 API:

- **RPC**: http://localhost:26657
- **REST API**: http://localhost:1317
- **gRPC**: localhost:9090

### 常用 API 示例

```bash
# 获取最新区块
curl http://localhost:1317/cosmos/base/tendermint/v1beta1/blocks/latest

# 查询节点状态
curl http://localhost:26657/status

# 查询账户余额
curl http://localhost:1317/cosmos/bank/v1beta1/balances/<地址>
```

## 📚 下一步

1. **阅读完整教程**

   ```bash
   cat ~/work/web3/chain-st/TUTORIAL.md
   ```

2. **查看项目文档**

   ```bash
   cat ~/work/web3/chain-st/chainst/README_CN.md
   ```

3. **尝试添加新功能**
   - 添加新的消息类型
   - 创建新的查询
   - 添加新的模块

## ❗ 常见问题

### Q: 如何停止区块链?

A: 在运行 `ignite chain serve` 的终端按 `Ctrl + C`

### Q: 如何重置区块链数据?

A:

```bash
cd ~/work/web3/chain-st/chainst
ignite chain clean
```

### Q: 端口被占用怎么办?

A:

```bash
# 查找占用进程
lsof -i :26657
lsof -i :1317

# 杀死进程
kill -9 <PID>
```

### Q: 如何查看完整的命令选项?

A:

```bash
chainstd --help
chainstd tx --help
chainstd query --help
```

## 🎯 功能清单

你的区块链已经实现了:

✅ 代币铸造 - 可以创建新的代币
✅ 代币转账 - 账户间转移代币
✅ 用户管理 - 创建和管理账户
✅ 区块查询 - 查看区块信息
✅ 交易查询 - 查看交易详情
✅ PoS 共识 - Tendermint 共识引擎
✅ REST API - HTTP 接口
✅ gRPC API - 高性能 RPC 接口
✅ Web 浏览器 - 可视化界面

## 🔗 资源链接

- Cosmos SDK 文档: https://docs.cosmos.network/
- Tendermint 文档: https://docs.tendermint.com/
- Ignite CLI 文档: https://docs.ignite.com/

## 💡 提示

- 区块链会自动每 12 秒产生一个新区块
- 所有操作都需要使用 `--chain-id chainst` 参数
- 测试环境的私钥保存在 `~/.chainst` 目录
- 区块链浏览器每 30 秒自动刷新

---

祝你开发愉快!如有问题,请查看:

- TUTORIAL.md - 详细教程
- README_CN.md - 完整文档
